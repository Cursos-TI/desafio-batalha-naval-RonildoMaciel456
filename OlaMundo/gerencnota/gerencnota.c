#include <stdio.h>

int main() {

    /*
    int opcao;
    float nota1, nota2, media;

    printf("Menu de Gerenciamento de Estudantes\n");
    printf("1. Calcular a Média\n");
    printf("2. Determinar Status\n");
    printf("3. Sair\n");
    printf("Escolha uma Opção: ");
    scanf("%d",&opcao);
    printf("\n");

    switch (opcao)
    {
    case 1:
        printf("Calcular a Média\n");
        printf("Digie a 1º Nota: ");
        scanf("%f",&nota1);
        printf("Digite a 2º Nota: ");
        scanf("%f",nota2);
        
        //Testar a condição se a nota1 >=0 && <=10:
        if ((nota1 >=0 && nota1 <= 10) && (nota2 >=0 && nota2 <=10)) {
            media = (nota1 + nota2) / 2;
            printf("A média é: %.2f", media);
        } else {
            printf("Entrada com valores errados de notas!\n");
        }
        break;
    case 2:
        printf("Determinar Status\n");
        printf("Digite o valor da Média: ");
        scanf("%f",&media);
        if ((media >=0 && media <= 10)) {
            if (media >= 7) 
            {
               printf("Aprovado!\n");
            }  
            else if (media >=5)
            {
               printf("Recuperação!\n");
            } else {
               printf("Reprovado!");
            }
        } else {
            printf("O valor da Média digitado é inválido ");
        }
        break;
    case 3:
        printf("Saindo do programa...\n");
        break;
    default:
        printf("Opção Inválida!\n");
        break;
        
    }
    */
    

    return 0;

}